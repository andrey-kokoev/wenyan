# Benchmarks

v1.0 benchmark commands:

- `pnpm benchmark:toy`
- `pnpm benchmark:toy:check`
- `pnpm benchmark:stress`

Toy profile is CI-blocking; stress profile is informational and artifact-driven.
