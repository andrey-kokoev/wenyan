# Frontend App Configuration
# Use '/' for same-origin requests (Worker serves both frontend and API)
# Or use full URL for separate backend: http://localhost:8787
VITE_API_URL=http://localhost:8787
VITE_APP_URL=http://localhost:5175
