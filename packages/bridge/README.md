# @wenyan/bridge

Foreign protocol bridge runtime for Wenyan.

- Standalone Node process (`wenyan-bridge`)
- Production NATS adapter
- Staged Kafka/MQTT adapters with contract-complete behavior
- Archive-backed rejection, sync state, and outbound retry queue
