{
  "name": "@wenyan/benchmark",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist/**/*", "README.md"],
  "private": true,
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run src",
    "benchmark:toy": "tsx src/throughput.ts --profile toy && tsx src/latency.ts --profile toy",
    "benchmark:toy:check": "tsx src/throughput.ts --profile toy --check && tsx src/latency.ts --profile toy --check",
    "benchmark:stress": "tsx src/throughput.ts --profile stress && tsx src/latency.ts --profile stress && tsx src/scale.ts --profile stress"
  },
  "dependencies": {
    "@wenyan/seal": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.2.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
