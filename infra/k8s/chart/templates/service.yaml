apiVersion: v1
kind: Service
metadata:
  name: wenyan
spec:
  type: {{ .Values.service.type }}
  selector:
    app: wenyan
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
